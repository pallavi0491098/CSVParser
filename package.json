{
  "name": "csvparser",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clean": "rimraf dist/*",
    "test": "jest 'src.[^.]*\\.test.*' --testResultsProcessor jest-json-reporter --config=jest.config.js",
    "tsc": "tsc",
    "lint": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "npm-run-all clean lint tsc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/jest": "^26.0.22",
    "@types/node": "^14.14.37",
    "jest": "^26.6.3",
    "jest-html-reporter": "^3.3.0",
    "jest-json-reporter": "^1.2.2",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.5.4",
    "typescript": "^4.2.3"
  },
  "dependencies": {
    "fs": "0.0.1-security",
    "path": "^0.12.7"
  }
}
